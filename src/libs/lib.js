import React, { Fragment } from "react";
import Formik from "./Formik";

const Lib = () => {
  return (
    <Fragment>
      {/* Formik */}
      <Formik />
    </Fragment>
  );
};

export default Lib;
